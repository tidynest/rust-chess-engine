[package]
name = "chess-core"
version = "0.1.0"
edition = "2021"

[dependencies]
chess = { workspace = true }
serde = { workspace = true, optional = true }
thiserror = { workspace = true }
anyhow = "1.0.100"

[features]
default = []
serde = ["dep:serde"]